Testbench:

iverilog -DUSE_POWER_PINS -DSIM gf180mcu_ocd_sram_test_tb.v > & iverilog.out

Currently the testbench does not run because "inout" ports are not allowed
to be driven by registers.  This needs to be corrected.
